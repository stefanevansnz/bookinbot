events:
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "bookings/{id}"
        cors: true            
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: post
        path: "booking"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: delete
        path: "booking"
        cors: true  
