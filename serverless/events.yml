events:
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "resources"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "resource/{id}"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "resource/{id}/{ownerid}"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: post
        path: "resources"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: delete
        path: "resources"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "bookings/{id}"
        cors: true            
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "bookings/{id}/{ownerid}"
        cors: true            
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: post
        path: "bookings"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: delete
        path: "bookings"
        cors: true
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "sharessearch/{id}/{email}"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "sharesresend/{id}"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "shares/{id}"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "shares"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: post
        path: "shares"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: delete
        path: "shares"
        cors: true  
