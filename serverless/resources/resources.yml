events:
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "resources"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: post
        path: "resource"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: delete
        path: "resource"
        cors: true  
