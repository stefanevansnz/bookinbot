events:
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "resources"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: get
        path: "resources/{id}"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: post
        path: "resources"
        cors: true  
    - http: 
        authorizer: ${self:custom.cognitoAuthorizer}
        method: delete
        path: "resources"
        cors: true  
